<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zpy.mybatis.BaseDao">
	<select id="getUser" parameterType="int" resultType="zpy.mybatis.User">
		select * from t_zpy WHERE id = #{id};
	</select>
</mapper>